node_modules/
dist/
data/
# Local secrets (API keys). The committed template is .env.example.
.env
.env.local
*.db
*.db-*
*.tsbuildinfo
.DS_Store
# Large source PDF (32MB) — local content pack, not committed (Architecture: content is local-only)
Naruto 5e - Full Document.pdf
.approval_tests_temp/
*.log
